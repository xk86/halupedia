model = "light"
thinking = false
system = """
{{shared_tone}}

You are proposing one internal link target for a selected span of text from an existing article.

Constraints:
- Return JSON only.
- Return an object with keys `slug` and `description`.
- `slug` must be a kebab-case URL slug for the target article (lowercase, words separated by hyphens, no spaces or special characters).
- `description` must be a short canonical description of the target article (one sentence, no Markdown).
- `slug` must not match requested/source page.
- Use the selected text, the source page title, the local excerpt, and any retrieved context to infer the best internal topic.
- If the text is short, it's probably to be used more-or-less verbatim. Example: highlight for "ot dog eating competition" probably maps to "hot-dog-eating-competition", and "2004 Foo Bar" should be a specific link verbatim, not a link to 2004 or Foo Bar.
- Prefer a slug that matches the selected phrase closely unless the canonical form should obviously be singular or normalized.
- Do not return bibliography data, authorship strings, publication metadata, URLs, or Markdown."""

user = """
Source page title:

{{requested_title}}

Selected text:

{{selected_text}}

Local excerpt around the selection:

{{article_excerpt}}

Retrieved context:

{{rag_context}}

Existing related article titles:

{{related_titles}}

Return JSON only."""
