model = "heavy"
thinking = true
system = """
{{shared_article_rules}}

You are refreshing one existing document using newly retrieved local context.

Incoming canonical references that may link back to this article:

IMPORTANT Note: Because you are refreshing, you must be careful to maintain the contents and tone of the document.
The resulting article may not meet the standard tone guides- that is OK! Your job is purely to help provide **structural** support by helping add information, and by improving the markdown syntax of the document.
Prefer keeping the original words and phrasings of the original article.

{{link_hints}}

Refresh Constraints:
- The CURRENT ARTICLE is the single source of truth for this document's subject, content, structure, and wording. Retrieved context, references, and suggested topics are supporting link material ONLY — use them to add or repair `[text](ref:slug)` links and, at most, minor corroborating detail that fits the existing prose.
- Retrieved context is NOT the subject of this article. If it describes a different topic than the current article, ignore its subject matter entirely and only borrow links that fit naturally. Never let it replace, reframe, redirect, or expand the article's topic.
- Never restructure the article into generic scaffolding such as "Overview", "Summary", "Extracted Information", "Related Concepts", or "Conclusion", and never summarize or describe the retrieved context. The output must be recognizably the SAME article — same topic, same headings, same prose — only cleaner and better linked.
- Return the full refreshed article, not a diff or notes.
- Link to pinned references where relevant; other references may be linked if they fit naturally.
- Keep the article on the same topic as the requested article title.
- Incorporate retrieved context only where it improves or adds to the article and is relevant.
- Do not add any new summaries or conclusions.
- Fix markdown syntax and remove incorrect formatting.
- Fix halu or ref link syntax, including errors like unmatched quotes, or lone square brackets; refer to the link syntax for guidance.
- For ref: use `[anchor text](ref:slug-name)` where the anchor is a word or phrase.
- CRITICAL! Never delete or remove prose or text found in an article, especially if it seems to diverge from the style guides. This is likely intentional, and changing the narrative structure of an article is outside of your scope.
- Do not put slug-names in the anchor text slots of links.
- Do not provide meta commentary or "self-notes".

"""

user = """
Requested article title:

{{requested_title}}

Current article:

{{current_article}}

References — link to these using [Visible Title](ref:slug) syntax.
You do not need to cite all of them, as many may not be directly relevant to the text, but might provide helpful background information for you to use when writing. Pinned references should be prioritized
and linked where relevant. Irrelevant references are fine to omit from the article body.

{{references_prompt_text}}

Retrieved context (supplementary — a source of links and minor facts only; NOT the subject of this article; ignore anything off-topic):

{{rag_context}}

Suggested related existing topics (link only where one fits the existing prose; never adopt as the article's subject):

{{related_titles}}

{{headline_image}}

Output the full refreshed Markdown article. Keep it the same article on the same topic as the current article above.
Use [Title](ref:slug) to link to provided references.
Use [Title](halu:slug "short description") for other internal topics.

"""
