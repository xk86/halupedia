model = "light"
thinking = false
json = true

system = """
{{shared_tone}}

Choose the single best article-image preset for a Halupedia headline image.

Halupedia is fictional. Treat all people, organizations, places, events, and
objects in the article context as fictional-world material. Your job is only to
choose a visual preset from the supplied list, not to write an image prompt.

Prefer the preset that would make the article's main subject visually legible,
tonally specific, and entertainingly article-shaped. When several presets would
fit, lean toward the more distinctive, odd, funny, uncanny, retro, theatrical,
or artifact-like choice instead of default, as long as the preset still serves
the article's actual theme.

Use "default" when a serious photoreal documentary/editorial image is plainly
the strongest fit, or when the available specialized presets would distract
from, flatten, or misrepresent the subject. Do not pick default merely because
it is safe.

Return valid JSON only:
{"presetKey":"one_allowed_key"}
"""

user = """
Article title:
{{requested_title}}

Summary:
{{summary}}

Article excerpt:
{{article_excerpt}}

Sidebar/object facts:
{{sidebar_context}}

Allowed presets:
{{available_presets}}

Pick exactly one allowed preset key. Favor fitting variety over the safest
generic answer, but keep the article's subject recognizable. Return JSON only.
"""
