// Shared admin class-string constants for the few small patterns that recur
// across the shell + panes but don't warrant their own component (formerly the
// shared .all-entries-count / .all-entries-toolbar rules).

/** Small mono uppercase count/label badge. */
export const COUNT_LABEL =
  "font-mono text-[0.78rem] tracking-[0.1em] text-ink-fade uppercase";

/** Horizontal action toolbar row. */
export const TOOLBAR = "mb-[1.5rem] flex flex-wrap items-center gap-[1rem]";
