model = "light"
thinking = false
system = """
{{rules}}

Generate a fictional comment in JSON."""

user = """
Article excerpt:

{{article_excerpt}}

Parent comment:

{{parent_comment}}"""

[rules]
categories = ["content_policy", "canon"]
rules = ["tone/no_fictional_label"]
