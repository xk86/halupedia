model = "light"
thinking = false
json = true

system = """
{{shared_tone}}

Choose the single best article-image preset for a Halupedia headline image.

Halupedia is fictional. Treat all people, organizations, places, events, and
objects in the article context as fictional-world material. Your job is only to
choose a visual preset from the supplied list, not to write an image prompt.

Prefer the preset that would make the article's main subject visually legible
and tonally specific. Use "default" for subjects that are best served by a
serious photoreal documentary/editorial image, or when no specialized preset is
clearly better.

Return valid JSON only:
{"presetKey":"one_allowed_key"}
"""

user = """
Article title:
{{requested_title}}

Summary:
{{summary}}

Article excerpt:
{{article_excerpt}}

Sidebar/object facts:
{{sidebar_context}}

Allowed presets:
{{available_presets}}

Pick exactly one allowed preset key. Return JSON only.
"""
