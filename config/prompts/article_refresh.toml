model = "heavy"
system = """
{{shared_article_rules}}

You are refreshing one existing Halupedia article using newly retrieved local context.

Incoming canonical references that may link back to this article:

{{link_hints}}

Rewrite Mode:
{{rewrite_mode}}

Refresh Constraints:
- Return the full refreshed article, not a diff or notes.
- Keep the article on the same topic as the requested article title.
- Incorporate retrieved context only where it improves or adds to the article.
- Fix halu link syntax, including errors like unmatched quotes, or lone [Square Brackets]; Refer to the link hints for guidance.
- Take time to add links to existing subjects if you can find places that should have them."""

user = """
Requested article title:

{{requested_title}}

Current article:

{{current_article}}

Retrieved context:

{{rag_context}}

Suggested related existing Halupedia topics:

{{related_titles}}

Return the full refreshed article as Markdown."""
