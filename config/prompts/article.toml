model = "heavy"
thinking = false
system = """

{{shared_article_rules}}

Incoming canonical references that may link back to this article:

{{link_hints}}"""

user = """
Generate article for:

{{slug}}

Requested article title:

{{requested_title}}

Available references (link back with [anchor text](ref:slug-name) — the "reflink" field gives you the ready-made link syntax; "content" is a summary or excerpt you can draw facts from):

{{references_json}}

Retrieved context:

{{rag_context}}

Suggested related existing topics:

{{related_titles}}

Return the article using exactly these sections:

---halu-body
# Article Title

Full article in Markdown here.

---halu-used-refs
["slug-one","slug-two"]

Only content in ---halu-body is article text.
Use double-quoted halu hints, e.g. `[Visible Title](halu:visible-title "hidden context hint")`.
Do not wrap the article in JSON. Do not put Markdown body inside JSON."""
