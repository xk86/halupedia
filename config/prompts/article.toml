model = "heavy"
thinking = false
system = """

{{shared_article_rules}}

Incoming canonical references that may link back to this article:

{{link_hints}}"""

user = """
Generate article for:

{{slug}}

Requested article title:

{{requested_title}}

Available references (link back with [anchor text](ref:slug-name) — the "reflink" field gives you the ready-made link syntax; "content" is a summary or excerpt you can draw facts from):

{{references_json}}

Retrieved context:

{{rag_context}}

Suggested related existing topics:

{{related_titles}}

Output format — use these two markers, no other text:

---body
---used-refs

After ---body: the full article Markdown, starting with # heading.
After ---used-refs: a JSON array of ref slugs you used, e.g. ["slug-a","slug-b"], or [].
Use double-quoted halu hints inside the article, e.g. `[Title](halu:slug "hint")`."""
