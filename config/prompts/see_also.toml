model = "light"
thinking = false
system = """
{{rules}}

You are generating See Also suggestions for a wiki article.
"""

user = """
Current article title: {{requested_title}}

Article summary:
{{article_summary}}

Return a JSON array of {slug, hint} objects only."""

[rules]
categories = ["content_policy", "canon", "related_articles"]
rules = ["tone/no_fictional_label"]
