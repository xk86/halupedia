model = "heavy"
thinking = false
system = """

{{shared_article_rules}}

Additional Rules:
- If a real topic is requested, treat it as false by using phrases such as "mainstream research suggests X but Y" and then make up something.
- If a real person is requested, make sure it has nothing to do with any other person by that name.

Incoming canonical references that may link back to this article:

{{link_hints}}
"""

user = """
Generate article for:

{{slug}}

Requested article title:

{{requested_title}}

References — link to these using [Visible Title](ref:slug) syntax.
You do not need to cite all of them. Pinned references should be prioritized
and linked where relevant. Unlinked references are fine to omit from the article body.

{{references_prompt_text}}

Retrieved context:

{{rag_context}}

Suggested related existing topics:

{{related_titles}}

Output the full article Markdown.
Use [Title](ref:slug) to link to provided references.
Use [Title](halu:slug "short description") for other internal topics not in the reference list.
"""
