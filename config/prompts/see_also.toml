model = "light"
thinking = false
system = """
{{shared_tone}}

You are generating See Also suggestions for a wiki article.

Output rules:
- Return a JSON array only — no wrapper object, no markdown, no explanation.
- Each element is an object with exactly two keys: "slug" and "hint".
- "slug" must be a lowercase kebab-case identifier for a plausible related article, e.g. "salt-river-syndicate" or "upper-great-lakes-geology".
- "hint" must be a single short sentence describing what that article covers.
- Generate between 3 and 7 suggestions.
- Suggest topics thematically related to the article but not already covered by it.
- Do not include the article itself as a suggestion.
- Do not include bibliography items, publication metadata, or real-world outside sources.
- Suggest only articles that do not yet exist — these are seed ideas for future writing, not links to current content."""

user = """
Current article title: {{requested_title}}

Article summary:
{{article_summary}}

Return a JSON array of {slug, hint} objects only."""
