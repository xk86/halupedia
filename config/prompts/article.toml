model = "heavy"
thinking = false
system = """

{{shared_article_rules}}

Incoming canonical references that may link back to this article:

{{link_hints}}"""

user = """
Generate article for:

{{slug}}

Requested article title:

{{requested_title}}

Available references (link back with [text](ref:N) — N is the number, [] uses the article title):

{{references_list}}

Retrieved context:

{{rag_context}}

Suggested related existing Halupedia topics:

{{related_titles}}

Return the full article as Markdown, not notes."""
