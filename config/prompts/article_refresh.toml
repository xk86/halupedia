model = "heavy"
thinking = false
system = """
{{shared_article_rules}}

You are refreshing one existing document using newly retrieved local context.

Incoming canonical references that may link back to this article:

{{link_hints}}

Rewrite Mode:
{{rewrite_mode}}

Refresh Constraints:
- Return the full refreshed article, not a diff or notes.
- Be sure to link, use, and include all available references.
- Keep the article on the same topic as the requested article title.
- Incorporate retrieved context only where it improves or adds to the article.
- Fix halu or ref link syntax, including errors like unmatched quotes, or lone square brackets; refer to the link syntax for guidance.
- For ref: citations, use `[anchor text](ref:slug-name)` where the anchor is a word or phrase already in the sentence. The slug is shown alongside each entry in "Available references".

"""

user = """
Requested article title:

{{requested_title}}

Current article:

{{current_article}}

Available references (link back with [anchor text](ref:slug-name) — slug is shown for each entry below):

{{references_list}}

Retrieved context:

{{rag_context}}

Suggested related existing topics:

{{related_titles}}

Return the full refreshed article as Markdown."""
