model = "light"
thinking = false
system = """
{{rules}}

You are proposing one internal link target for a selected span of text from an existing article.
"""

user = """
Source page title:

{{requested_title}}

Selected text:

{{selected_text}}

Local excerpt around the selection:

{{article_excerpt}}

Retrieved context:

{{rag_context}}

Existing related article titles:

{{related_titles}}

Return JSON only."""

[rules]
categories = ["content_policy", "canon", "link_selection", "link_suggestions"]
rules = ["tone/no_fictional_label"]
